import React from 'react';

const Header = props => (
    <div className="container text-center">
        <h1 id="titleMaine"><b> South Park Clicky Game</b></h1>
        <br/>
        <h2 className="info">Click on a character! The goal is to only click a character once.</h2>
    </div>
)

export default Header;